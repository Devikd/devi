#include<stdio.h>
#include<string.h>
void main()
{
        char ss[10];
        int ii;
        scanf("%s",&s[ii]);
        int l=strlen(ss);
        for(ii=0;ii<l;ii++)
        {
                ss[ii]=ss[ii]+3;
                printf("%c",ss[ii]);
        }
}
